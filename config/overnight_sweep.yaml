# Overnight Session Strategy — Parameter Sweep Grid
#
# Keys match OvernightSessionStrategy params dict exactly.
# Grid is horizon-aware: wider fwd_return_threshold for longer horizons.
#
# Usage:
#   Referenced from config/research_loop.yaml under strategies[].sweep

fwd_return_threshold:
  min: 0.002
  max: 0.020
  step: 0.003

entry_window_minutes:
  values: [15, 30, 60]

horizon_seconds:
  values: [3600, 14400, 28800]

gate_on_risk_flow:
  values: [false, true]

gate_on_news_sentiment:
  values: [false, true]
